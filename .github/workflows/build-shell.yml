name: Build Shell 
run-name: ${{ github.actor }} is building the shell image
on:
  push: 
    paths:
      - 'shell/**'
jobs:
  Build-Shell-Image:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
      - name: Check out repository code
        uses: actions/checkout@v4
      - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        # Replace with Build & Push to registry action 
      - run: docker build --target shell -t monorepo:shell .
      - run: docker run --rm monorepo:shell ./app1/script.sh
      # Add run action like: curl --request GET  --url 'https://coolify.example.com/v1/deploy?tag=shell-script'  --header 'Authorization: Bearer demo-bearer-token'
      - run: echo "🍏 This job's status is ${{ job.status }}."
